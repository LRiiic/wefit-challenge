import { styled } from "styled-components";

export const Wrapper = styled.section({
    maxWidth: "1080px",
    padding: "0 16px",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    alignItems: "center",
    gap: "24px",
    margin: "0 auto",
});